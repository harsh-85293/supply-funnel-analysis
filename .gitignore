# virtual environments
.venv/
.venv*/
venv/

# python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# generated analysis output -- rebuild with `python run_all.py`
outputs/

# Note: the built deliverables in deliverables/ (MEMO.pdf, DECK.pdf, DECK.pptx)
# ARE committed, so a reviewer can open them without running the pipeline.
# Rebuild via build_pdfs.py and build_pptx.py.

# personal debrief prep -- not one of the three deliverables
deliverables/SPEAKER_NOTES.md

# os cruft
.DS_Store
Thumbs.db
